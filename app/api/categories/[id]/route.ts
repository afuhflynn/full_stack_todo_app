import { 
  GET
} from "@/server/controllers/category-controller/category.id.controller"; 

/**
 * ====================================================================
 * all the other grud operations has been moved to the parent route.ts 
 * only GET is remaining here because that's standard convention 
 * ====================================================================
 */

export { 
  GET
}